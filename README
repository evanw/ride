To run the server:

    cd webapp && python build.py release && cd www && python server.py

TODO:

		Broadcast channel to fix echo problem, don't send all movements while dragging because it'll mess up the undo stack.
